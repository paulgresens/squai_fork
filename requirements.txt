wheel
torch>=2.1.0
transformers>=4.30.0
numpy>=1.24.0
tqdm>=4.65.0
faiss-cpu>=1.7.4  # or faiss-gpu for GPU support
sentence-transformers>=2.2.2
datasets>=2.12.0
rouge>=1.0.1
pinecone
boto3==1.35.88
opensearch-py==2.8.0
nltk
accelerate
# farm-haystack
uvicorn
streamlit
fastapi
#llama-index
#llama-index-core<0.14.15 pin: llama-index==0.14.0 doesn't pin core tightly, so pip
#resolves the latest 0.14.x core. 0.14.15 introduced `llm: LLM | None = None` as a
#real (non-deferred) annotation in prompt_helper.py, which is PEP 604 syntax and
#errors on Python 3.9. 0.14.0-0.14.14 don't have it.
llama-index==0.14.0
llama-index-core<0.14.15
llama-index-retrievers-bm25
python-dotenv

# language detection
langdetect

#for other models
sentencepiece
protobuf

#for cosine similarity
sentence_transformers>=3.0.0

#for 4 bit 
bitsandbytes
rank_bm25

#dataset generation
requests

#for answer correctness evaluation (ragas)
#ragas>=0.4.0 unconditionally imports langchain_community.chat_models.vertexai,
#which was removed from langchain-community>=0.4. Pin both until ragas fixes this upstream.
#https://github.com/vibrantlabsai/ragas/issues/2745
ragas==0.3.9
langchain-community<0.4
openai
banks<2.4.4


#cleaning
rapidfuzz 

# --- Hardware Acceleration (The "Fast Path") ---
# Use --no-build-isolation to prevent version conflicts during compilation
# causal-conv1d>=1.6.0
# flash-linear-attention>=0.4.0
# flash-attn>=2.7.0

# # --- Build Tools (Required to compile the kernels above) ---
# ninja
# packaging
requests


#test if this fixes the plyvel issues
plyvel-ci